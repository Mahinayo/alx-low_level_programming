#include "main.h"
/**
 * _isalpha - return 1 if is it alpha
 * @c: take char
 * Return: 0 if it is not alpha
 */
int _isalpha(int c
{
if (c >= 'A' && c <= 'Z') || (c >= 'a' && c<= 'z')
{
return (1);
}
else
{
return (0);
}
}

		
